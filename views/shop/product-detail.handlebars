<main>
    <div style="display: flex; justify-content: center">
        <article class="card">
            <header class="card__header">
                <h1 class="product__title">{{ prod.title }}</h1>
            </header>
            <div class="card__image">
                <img src="{{ prod.imageUrl }}" alt="books"></img>
            </div>
            <div class="card__content">
                <h2 class="product__price">£{{ prod.price }}</h2>
                <p class="product__description">{{ this.description }}</p>
            </div>
            <div class="card__actions">
                <button class="btn"><a href="/products/{{prod.id}}">Details</a></button>
                <form action="/add-to-cart" method="POST">
                    <button class="btn">Add to Cart</button>
                </form>
            </div>
        </article>
    </div>
</main>